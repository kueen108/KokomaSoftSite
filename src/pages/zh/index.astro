---
import Layout from '../../layouts/Layout.astro';
import HomePage from '../../components/HomePage.astro';
import { apps } from '../../i18n/apps';
import {
  appItemListSchema,
  homeAlternates,
  homeDescription,
  homeTitle,
  organizationSchema,
  websiteSchema,
} from '../../lib/seo';
---

<Layout
  lang="zh"
  title={homeTitle('zh')}
  description={homeDescription('zh')}
  canonicalPath="/zh/"
  alternates={homeAlternates()}
  xDefaultPath="/"
  image="/app-icons/ungyeol.png"
  structuredData={[organizationSchema('zh'), websiteSchema('zh'), appItemListSchema(apps, 'zh')]}
  theme="dark"
>
  <HomePage lang="zh" />
</Layout>
